<launch>
    <!-- Variablen Definition -->
    <let name="urdf_path" 
         value="$(find-pkg-share telerobot_description)/urdf/my_robot.urdf.xacro" />
    <let name="rviz_config_path"
         value="$(find-pkg-share telerobot_bringup)/rviz/urdf_config_slam.rviz" />
    <let name="slam_params_file"
         value="$(find-pkg-share telerobot_bringup)/nav2/mapper_params_online_async.yaml" />
    <let name="map_pgm_file"
         value="$(find-pkg-share telerobot_bringup)/nav2/maps/school.pgm" />
    <let name="map_yaml_file"
         value="$(find-pkg-share telerobot_bringup)/nav2/maps/school.yaml" />

    <!-- Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description"
               value="$(command 'xacro $(var urdf_path)')" />
    </node>

    <!-- Gazebo Simulation -->
    <include file="$(find-pkg-share gazebo_ros)/launch/gazebo.launch.py">
        <arg name="world" value="$(find-pkg-share telerobot_bringup)/worlds/school.sdf" />
    </include>

    <!-- SLAM Toolbox -->
    <include file="$(find-pkg-share slam_toolbox)/launch/online_async_launch.py">
        <arg name="slam_params_file" value="$(var slam_params_file)" />
        <arg name="use_sim_time" value="true" />
    </include>

    <!-- Map Server-->
   <node pkg="nav2_map_server" exed="map_server" args="$(var map_yaml_file)" respawn="true" name="map1" />

    <!-- Robot Spawn in Gazebo -->
    <node pkg="gazebo_ros" exec="spawn_entity.py"
          args="-topic robot_description -entity my_robot" />

    <!-- RViz -->
    <node pkg="rviz2" exec="rviz2" output="screen" 
          args="-d $(var rviz_config_path)" />
</launch>